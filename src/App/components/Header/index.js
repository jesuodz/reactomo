import React from 'react';

import './index.css';

const Header = () => (
  <header className="header">
    <div className="header-wrapper">
      <h1 className="title-header">reactomo</h1>
    </div>
  </header>
);

export default Header;
